export const POST_IMAGES_BUCKET = 'post-images';

export const POINT_TYPES: PointTypes = {
	height: 'Plant height',
	flower: 'Flower amount',
	fruit: 'Ripe fruit amount',
};

type PointTypes = {
	height: string;
	flower: string;
	fruit: string;
};
